import { ReactElement } from "react";

import { SpawnEditorUnpackForm } from "@/applications/spawn-editor/components/SpawnEditorUnpackForm";

export function SpawnEditorUnpackPage(): ReactElement {
  return <SpawnEditorUnpackForm />;
}
