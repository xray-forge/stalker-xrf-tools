#[derive(Default)]
pub struct GamedataProjectVerificationResult {
  pub duration: u128,
  pub is_valid: bool,
}

#[derive(Default)]
pub struct GamedataProjectWeaponVerificationResult {
  pub is_valid: bool,
}
